let firstName = 'Muhammad';
let lastName = 'Salihu'
let age = 77;
console.log(firstName, lastName, age); //to get the output on your browser console.


// We use a variable to store data temporarily in a computer's memory, so we store our data somewhere and we give that memory location a name , with this name we can read the data at a given location in the future. e.g boxes ur to put diff item and give them labels , what we put inside the box is the value we assign to the variable that is the data, the label we put on the box is the name of our variable
// Rules of naming a variable:
// - Cannot be a reserved keyword
// - They should be meaningful and descriptive names
// -They cannot start with a number (1name)
// -They cannot contain a space or hyphen (- or _)
// use camel notations (firstName)
// they are case-sensitive
// Declare a variable using (,) or create it on a separate line