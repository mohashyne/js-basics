//implemet function isLandscape with the width and height of an image , and return true if the image isLandscape width is greater than height esle  return false.

console.log(isLandscape(800, 600));

function isLandscape(width, height) {
    return (width > height);

}